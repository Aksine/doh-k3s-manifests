apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nginx-rtmp.fullname" . }}-test-connection"
  labels:
    {{- include "nginx-rtmp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nginx-rtmp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
