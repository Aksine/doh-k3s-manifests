---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gittea-pvc
  namespace: default
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  volumeMode: Filesystem
  # volumeName should be same as PV name
  volumeName: gittea-pv
  storageClassName: csi-cephfs-sc